+ <<: &base
+   seed: 42
+   splits: {train_ratio: 0.90, val_ratio: 0.05, test_ratio: 0.05}
+   targets: {total_examples: 83000}
+   include: {skyrim: true}
+   options:
+     light_max_pairs: 12000
+     ed_max_examples: 5000
+     attach_uesp_world_facts: true
+     respect_upstream_splits: true
+     max_scenario_length: 400
+     max_fact_length: 200
+     max_turns_per_dialogue: 20
+ weights:
+   personachat: 0.12
+   spc:         0.18
+   charcodex:   0.24
+   light:       0.14
+   ed:          0.06
+ ablation_weights:
+   skyrim: 0.26   # ~22k of 83k total reserved for classwork-only shard